export enum USER_STATUS {
    ACTIVE = 'active',
    PENDING = 'pending',
    INACTIVE = 'inactive',
    BANNED = 'banned',
    DELETED = 'deleted',
}

export enum USER_ROLE {
    ADMIN = 'admin',
    USER = 'user',
}

export enum USER_GENDER {
    MALE = 'MALE',
    FEMALE = 'FEMALE',
    OTHER = 'OTHER',
}
